# ── Core ─────────────────────────────────────────────────────
# Loose pins so google-adk (which requires fastapi>=0.124.1) can co-exist.
fastapi>=0.124.1,<1.0
uvicorn[standard]>=0.34.2,<1.0
pydantic>=2.11.3,<3.0
pydantic-settings>=2.9.1,<3.0

# ── Database ─────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.41
asyncpg==0.31.0
psycopg2-binary==2.9.10
alembic==1.15.2
greenlet==3.2.1

# ── ChromaDB ─────────────────────────────────────────────────
chromadb==0.6.3

# ── HTTP / Async ─────────────────────────────────────────────
httpx==0.28.1

# ── Self-monitor — docker socket access for container stats ──
docker==7.1.0
# Process/runtime introspection for parity-self process category metrics.
psutil>=5.9

# ── YAML ─────────────────────────────────────────────────────
pyyaml==6.0.2

# ── Logging ──────────────────────────────────────────────────
structlog==25.1.0

# ── Gemini on Vertex AI ──────────────────────────────────────
# Used by backend.integrations.gemini.GeminiClient for one-shot LLM
# calls. The ADK agents (Rewire 2 onward) use the same SDK underneath.
google-genai>=1.0.0,<2.0.0

# ── Google Agent Development Kit ─────────────────────────────
# LlmAgent + SequentialAgent + Runner for the chat assistant and
# the Detect → Investigate → Reason pipeline.
google-adk>=1.33.0,<2.0.0

# ── MCP client (talks to the Dynatrace MCP server) ───────────
mcp>=1.0

# ── Scheduling ───────────────────────────────────────────────
apscheduler==3.11.0

# ── Network Automation ───────────────────────────────────────
pyats[full]==25.2

# ── SNMP polling ─────────────────────────────────────────────
# pysnmp-lextudio is the maintained async-capable fork of pysnmp; ships
# the SNMPv2c walker used by services/snmp_poller.py to feed Dynatrace's
# metrics-ingest path with parity.snmp.* timeseries every 60s.
pysnmp-lextudio>=6.0.0,<7.0
